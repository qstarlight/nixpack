{
  "name": "nixpack-project",
  "version": "1.0.0",
  "description": "Railway Nixpack project with SSHX",
  "scripts": {
    "start": "npm run sshx && node index.tsx",  
    "sshx": "curl -sSf https://sshx.io/get | sh && sshx",
    "build": "npx tsc",  
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": "18.x"
  },
  "type": "module"
}
